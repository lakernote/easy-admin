<?xml version="1.0" encoding="UTF-8"?>
<!-- scan:是否动态监控 scanPeriod:监控周期 debug:是否打印logback内部日志 -->
<configuration scan="true" scanPeriod="60 seconds" debug="false">
    <property name="LOG_HOME" value="logs"/>
    <property name="ENCODING" value="UTF-8"/>

    <appender name="DEFAULT" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 正在记录的日志文件的路径及文件名 -->
        <file>${LOG_HOME}/laker.log</file>
        <!-- 将日志事件追加到已存在的日志文件中 -->
        <Append>true</Append>
        <!-- 谨慎模式 默认关闭-->
        <prudent>false</prudent>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--日志打印的格式-->
            <pattern>%d{HH:mm:ss.SSS} [%t] %-5level %logger{50} %line - %m%n</pattern>
            <!--编码格式-->
            <charset>${ENCODING}</charset>
        </encoder>
        <!-- 按天回滚 daily -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 每天日志归档路径以及格式 -->
            <FileNamePattern>${LOG_HOME}/%d{yyyy-MM-dd}/laker-%i.log</FileNamePattern>
            <!--日志文件保留天数-->
            <maxHistory>15</maxHistory>
            <!--日志量最大20GB-->
            <totalSizeCap>20GB</totalSizeCap>
            <!--超过10MB就切分日志-->
            <maxFileSize>10MB</maxFileSize>
        </rollingPolicy>
    </appender>

    <!-- 日志输出格式 TRACE_LOG_PATTERN-->
    <property name="TRACE_LOG_PATTERN"
              value="%d{HH:mm:ss.SSS} [%thread] %-5level [%X{userId}|%X{traceId}] %logger{20} - [%method,%line] - %msg%n"/>


    <!-- 彩色格式 -->
    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{HH:mm:ss.SSS}){faint} %clr(%5p) %clr(---){faint} %clr([%15.15t]) [%X{userId:-},%X{traceID:-},%X{traceId:-},%X{spanId:-}] %clr(%-60.60logger{59}){cyan} %clr(:%line){cyan} %m%n%wEx"/>
    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx"
                    converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <!-- 控制台输出 -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <!--            <pattern>%clr(%d{HH:mm:ss.SSS}){faint} %clr(%5p) %clr(-&#45;&#45;){faint} %clr([%15.15t]) [%X{userId}|%X{traceId}] %clr(%-60.60logger{59}){cyan} %clr(:%line){cyan} %m%n%wEx{full,-->
            <!--                java.lang.reflect.Method,-->
            <!--                org.springframework.aop,-->
            <!--                org.springframework.security,-->
            <!--                org.springframework.transaction,-->
            <!--                org.springframework.easy-admin-client,-->
            <!--                sun.reflect,-->
            <!--                net.sf.cglib,-->
            <!--                org.springframework.cglib.proxy,-->
            <!--                net.bull.javamelody-->
            <!--                }</pattern>-->
        </encoder>
    </appender>

    <!-- 异步输出  异步日志输出之后，日志信息将暂存在 ArrayBlockingQueue 列表中，后台会有一个 Worker 线程不断地获取缓冲区内容，然后写入磁盘中。-->
    <appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <!-- 当 queueSize 快达到上限时，可以通过配置，丢弃一些级别比较低的日志，这个值默认是队列长度的 80%；但若你担心可能会丢失业务日志，则可以将这个值设置成 0，表示所有的日志都要打印。-->
        <discardingThreshold>0</discardingThreshold>
        <!--队列大小 代表了队列的大小，默认是256。如果这个值设置的太大，大日志量下突然断电，会丢掉缓冲区的内容；-->
        <queueSize>512</queueSize>
        <!--最大刷新时间，单位是毫秒，默认是 1000 毫秒，也就是 1 秒。-->
        <!-- 关闭日志上下文后，继续执行写任务的时间，这是通过调用 Thread 类的 join 方法来实现的（worker.join(maxFlushTime)）-->
        <maxFlushTime>1000</maxFlushTime>
        <!--这里指定了一个已有的Appender-->
        <appender-ref ref="DEFAULT"/>
    </appender>

    <!-- 以下针对不同的模块需求，设置不同的日志级别去过滤打印-->
    <logger name="com.test.demo" level="DEBUG">
        <appender-ref ref="DEFAULT"/>
    </logger>

    <!-- 定义com.test.demo模块的包，只输出INFO级别的日志,additivity 属性为 false，这样就不会继承 <root> 的 Appender 了 -->
    <logger name="com.test.demo" level="INFO" additivity="false">
        <appender-ref ref="DEFAULT"/>
    </logger>

    <!-- 定义org.snaker.engine模块的包，只输出ERROR级别的日志-->
    <logger name="org.snaker.engine" level="ERROR"/>
    <!-- 不支持通配符 com.laker.admin.module.**.mapper-->
    <logger name="com.laker.admin.module" level="INFO"/>
    <logger name="com.laker.admin.framework" level="INFO"/>
    <logger name="io.micrometer.core.instrument.logging" level="WARN"/>
    <logger name="org.apache.kafka.clients" level="WARN"/>

    <!-- 定义p6spy sql监控 -->
    <logger name="p6spy" level="INFO"/>
    <!-- 默认日志输出级别  WARN INFO-->
    <root level="INFO">
        <appender-ref ref="DEFAULT"/>
        <appender-ref ref="console"/>
    </root>
</configuration>